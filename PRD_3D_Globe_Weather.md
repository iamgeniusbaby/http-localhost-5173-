# PRD — 3D 지구본 랜드마크 날씨 사이트 (가칭: **Terra Weather**)

| 항목 | 내용 |
|---|---|
| 문서 버전 | v0.1 (Draft) |
| 작성일 | 2026-08-18 |
| 상태 | 리뷰 대기 |
| 데이터 소스 | Open-Meteo Forecast API / Geocoding API |

---

## 1. 개요 (Overview)

우주 공간에 떠 있는 입체적인 지구본 위에 각국의 랜드마크가 핀으로 표시되고, 클릭하면 해당 위치의 **실시간 날씨와 현지 시각**을 보여준다. 동시에 지구본 뒤의 **배경(우주 공간 → 현지 하늘)** 이 선택된 지점의 시간대와 날씨에 맞춰 전환되어, "그 도시로 순간이동한 듯한" 경험을 제공한다.

기존 날씨 사이트가 *정보 조회 도구*라면, 이 제품은 **정보 + 체험**을 결합한 인터랙티브 웨더 익스플로러다.

### 한 줄 정의
> 지구본을 돌려 랜드마크를 찍으면, 그곳의 날씨와 시간 속으로 화면이 바뀌는 날씨 사이트.

---

## 2. 배경 및 문제 정의

| 구분 | 내용 |
|---|---|
| 문제 | 기존 날씨 서비스는 텍스트/숫자 위주라 "다른 나라의 지금"을 감각적으로 느끼기 어렵다. 여러 도시를 비교하려면 탭을 여러 개 열어야 한다. |
| 기회 | Open-Meteo는 API 키 없이 무료로 전 세계 날씨 + 타임존 + 지오코딩을 제공한다. WebGL은 데스크톱/모바일 모두에서 충분히 성숙했다. |
| 가설 | "정보 밀도"보다 "몰입감"을 앞세우면, 날씨 조회라는 반복 행동에 재방문·체류·공유 동기가 생긴다. |

---

## 3. 목표 (Goals) / 비목표 (Non-Goals)

### 3.1 목표
- **G1.** 랜드마크 클릭 → 3초 이내에 날씨·시간·배경 전환이 완료되는 몰입형 인터랙션 제공
- **G2.** 지구본을 쓰지 않아도 목적을 달성할 수 있는 대체 경로(검색 / 리스트 / 필터) 제공
- **G3.** 저사양 기기·느린 네트워크·API 장애 상황에서도 우아하게 동작 (로딩·에러 상태 1급 시민 취급)
- **G4.** 첫 화면 LCP 2.5초 이내, 인터랙션 중 평균 45FPS 이상

### 3.2 비목표 (v1 제외)
- 회원가입 / 계정 시스템 (즐겨찾기는 localStorage로 처리)
- 기상 예보 해설, 미세먼지/대기질, 태풍 경로 등 전문 기상 콘텐츠
- 사용자 제작 랜드마크 등록(UGC)
- 네이티브 앱, 알림 푸시
- 수익화 (Open-Meteo 무료 티어는 **비상업적 용도** 한정)

---

## 4. 타깃 사용자

| 페르소나 | 상황 | 니즈 |
|---|---|---|
| **여행 계획자 (주)** | 다음 달 유럽 여행지 3곳을 두고 고민 중 | 여러 도시 날씨를 한 화면에서 비교, 체감 기온 |
| **원격 협업자** | 해외 동료와 미팅 시간 조율 | 현지 시각 + 낮/밤 여부를 직관적으로 확인 |
| **탐험형 방문자** | 특별한 목적 없이 유입 | 돌려보고, 예쁜 화면을 캡처해 공유 |

---

## 5. 성공 지표 (Success Metrics)

| 지표 | 목표치 (출시 후 8주) |
|---|---|
| 세션당 평균 랜드마크 조회 수 | ≥ 3.5개 |
| 평균 체류 시간 | ≥ 90초 |
| 검색/필터/리스트 중 1개 이상 사용한 세션 비율 | ≥ 35% |
| 재방문율 (7일) | ≥ 20% |
| 3D 렌더 실패 → 2D 폴백 전환율 | ≤ 3% |
| API 에러 화면 노출률 | ≤ 1% |

---

## 6. 정보 구조 (IA)

```
/                       메인 (지구본 + 오버레이 UI)
  ├─ 상단바      : 로고 · 도시 검색창 · 단위 토글(°C/°F) · 뷰 전환(지구본/리스트)
  ├─ 좌측 패널   : 필터 (지역·날씨상태·기온대·시간대·즐겨찾기)
  ├─ 중앙        : 3D 지구본 + 랜드마크 핀
  ├─ 우측 패널   : 선택 지점 날씨 상세 카드 (기본 접힘 → 선택 시 슬라이드 인)
  ├─ 하단        : 즐겨찾기 도크 / 데이터 출처 크레딧
  └─ 배경 레이어 : 시간대 × 날씨 연동 씬 (지구본 뒤)

/list                   리스트 뷰 (동일 데이터, 카드/테이블)
/place/:slug            딥링크 (예: /place/eiffel-tower) — 새로고침·공유 시 상태 복원
```

**URL 상태 동기화:** 선택 지점, 필터, 단위는 쿼리스트링에 반영한다.
`/?place=eiffel-tower&filter=rain&unit=c`

---

## 7. 핵심 사용자 플로우

### 7.1 메인 플로우
1. 진입 → **로딩 화면** (지구 텍스처·모델·랜드마크 JSON 프리로드, 진행률 표시)
2. 지구본이 우주 배경 속에서 페이드인, 천천히 자전
3. 사용자가 드래그로 회전 / 휠로 줌
4. 랜드마크 핀 호버 → 이름 + 아이콘 + 기온 미니 툴팁
5. 클릭 → 카메라가 해당 지점으로 이동(약 1.2초 ease-out) + 날씨 API 호출
6. 로딩 중 상세 패널은 **스켈레톤** 표시
7. 응답 도착 → 상세 카드 슬라이드 인 + **배경이 우주 → 현지 하늘로 크로스페이드**
8. ESC / 배경 클릭 → 지구본 뷰로 복귀 (배경도 우주로 역전환)

### 7.2 검색 플로우
검색창 입력 → 300ms 디바운스 → Geocoding API 자동완성 → 선택 → 해당 좌표로 지구본 카메라 이동 → 7.1의 5단계 이후와 동일

### 7.3 리스트 플로우
리스트 뷰 전환 → 필터 적용 → 카드 클릭 → 지구본 뷰로 돌아가며 해당 지점 선택 상태로 진입

---

## 8. 기능 요구사항 (Functional Requirements)

### F1. 3D 지구본
| ID | 요구사항 | 우선순위 |
|---|---|---|
| F1.1 | 구체 지구 렌더링 (알베도 / 노멀 / 스펙큘러 / 야간 조명 텍스처) | P0 |
| F1.2 | 마우스 드래그·터치 스와이프 회전, 휠·핀치 줌 (줌 범위 제한) | P0 |
| F1.3 | 유휴 상태 시 자동 자전 (0.05 rad/s), 사용자 입력 시 정지 | P1 |
| F1.4 | 실제 태양 위치 기반 주야 경계선(터미네이터) 표현 — 현재 UTC 기준 계산 | P1 |
| F1.5 | 구름 레이어 (반투명 회전 셸) | P2 |
| F1.6 | 대기 산란(Atmosphere/Fresnel glow) 셰이더 | P1 |
| F1.7 | WebGL 미지원·컨텍스트 로스트 시 **2D 지도 폴백** 자동 전환 | P0 |

### F2. 랜드마크 핀
| ID | 요구사항 | 우선순위 |
|---|---|---|
| F2.1 | 랜드마크 JSON 기반 핀 배치 (위경도 → 구면 좌표 변환) | P0 |
| F2.2 | 지구 뒷면 핀은 자동 숨김/투명도 감소 (depth test) | P0 |
| F2.3 | 핀에 현재 날씨 아이콘 + 기온 배지 표시 (뷰포트 내 핀만 배치 조회) | P0 |
| F2.4 | 호버 툴팁, 클릭 시 선택 상태(펄스 링 애니메이션) | P0 |
| F2.5 | 줌 아웃 시 근접 핀 클러스터링 | P2 |
| F2.6 | 랜드마크 외 임의 지점 클릭 시 역지오코딩 기반 조회 | P2 |

**v1 랜드마크 세트 (12개, 대륙 균형 고려)**
에펠탑(파리) · 자유의여신상(뉴욕) · 콜로세움(로마) · 도쿄타워(도쿄) · 경복궁(서울) · 만리장성(베이징) · 타지마할(아그라) · 오페라하우스(시드니) · 크라이스트 리디머(리우) · 피라미드(기자) · 부르즈 할리파(두바이) · 마추픽추(쿠스코)
→ **확장 목표: v1.1에서 40개**. 데이터는 코드가 아닌 `landmarks.json`으로 분리해 추가 시 배포만 하면 되도록 설계.

### F3. 날씨 · 시간 상세 패널
| ID | 요구사항 | 우선순위 |
|---|---|---|
| F3.1 | 현재 기온 / 체감온도 / 날씨 상태(한글) / 아이콘 | P0 |
| F3.2 | **현지 시각** (실시간 초 단위 갱신) + UTC 오프셋 + 한국과의 시차 | P0 |
| F3.3 | 습도 · 풍속/풍향 · 강수량 · 기압 · 자외선지수 · 가시거리 | P1 |
| F3.4 | 일출/일몰 시각 + 낮 길이 프로그레스 바 | P1 |
| F3.5 | 시간별 예보 24시간 (수평 스크롤 차트) | P1 |
| F3.6 | 일별 예보 7일 (최고/최저 기온 범위 바) | P1 |
| F3.7 | °C/°F, m/s·km/h 단위 토글 (설정 유지) | P1 |
| F3.8 | 즐겨찾기 토글 (localStorage) | P1 |
| F3.9 | 공유하기 — 현재 상태 딥링크 복사 | P2 |

### F4. 배경 연동 연출 ★핵심 차별점
선택된 지점의 **로컬 시간대 × 날씨 코드**를 조합해 배경 씬을 결정한다.

**시간대 구간 (일출/일몰 데이터 기반 계산)**
`dawn(일출±1h)` / `day` / `dusk(일몰±1h)` / `night`

**배경 매트릭스**

| 날씨 그룹 | dawn | day | dusk | night |
|---|---|---|---|---|
| 맑음 (0–1) | 보라→주황 그라디언트, 낮은 태양 | 청명한 하늘, 렌즈 플레어 | 주황→남색, 실루엣 | 짙은 남색, 별 파티클 + 달 |
| 구름 (2–3) | 회보라, 구름 실루엣 | 회백색, 흐르는 구름 레이어 | 회주황 | 흐린 밤, 별 일부 가림 |
| 안개 (45,48) | 전 시간대 공통: 저채도 + 볼류메트릭 포그 + 블러 강화 |||
| 비 (51–67, 80–82) | 어두운 회청색 + **빗줄기 파티클** + 유리 물방울 셰이더 |||
| 눈 (71–77, 85–86) | 밝은 회백색 + **눈송이 파티클**(느린 낙하) + 비네트 |||
| 뇌우 (95–99) | 짙은 먹구름 + 랜덤 간격 **번쩍임 플래시** |||

| ID | 요구사항 | 우선순위 |
|---|---|---|
| F4.1 | 시간대 × 날씨 조합에 따른 배경 그라디언트/스카이박스 전환 | P0 |
| F4.2 | 전환은 800ms 크로스페이드 (급작스러운 점프 금지) | P0 |
| F4.3 | 날씨별 파티클 시스템 (비/눈/별) — 강도(precipitation) 값에 비례해 밀도 조절 | P1 |
| F4.4 | 지구본 조명(directional light) 색온도·강도도 배경과 동기화 | P1 |
| F4.5 | 앰비언트 사운드 (빗소리·바람·천둥) — **기본 OFF**, 사용자 토글 | P2 |
| F4.6 | `prefers-reduced-motion` 시 파티클·카메라 애니메이션 비활성, 정적 배경만 | P0 |

### F5. 도시 검색창 ★필수
| ID | 요구사항 | 우선순위 |
|---|---|---|
| F5.1 | 상단 고정 검색 인풋, 플레이스홀더 "도시나 랜드마크를 검색하세요" | P0 |
| F5.2 | 300ms 디바운스 후 Open-Meteo Geocoding API 호출 (`count=8`, `language=ko`) | P0 |
| F5.3 | 자동완성 드롭다운: `도시명 · 행정구역, 국가` + 국기 이모지 + 인구 | P0 |
| F5.4 | **내부 랜드마크 우선 매칭** — 랜드마크명이 일치하면 지오코딩 결과보다 상단 노출 | P0 |
| F5.5 | 키보드 내비게이션 (↑↓ 이동, Enter 선택, Esc 닫기), ARIA combobox 패턴 | P0 |
| F5.6 | 최근 검색 5건 저장 및 포커스 시 표시 (localStorage) | P1 |
| F5.7 | 결과 없음 상태: "'{검색어}'에 대한 결과가 없습니다. 철자를 확인하거나 영문으로 검색해 보세요." + 추천 도시 3개 | P0 |
| F5.8 | 검색 실패(네트워크) 시 인풋 하단 인라인 에러 + 재시도 버튼 (전체 에러 화면으로 넘기지 않음) | P0 |
| F5.9 | 단축키 `/` 또는 `Cmd/Ctrl+K`로 검색창 포커스 | P2 |

### F6. 필터 ★필수
현재 로드된 랜드마크/도시 목록을 기준으로 필터링하며, 지구본 핀과 리스트 뷰에 **동시 적용**된다.

| 필터 | 옵션 | 우선순위 |
|---|---|---|
| 대륙/지역 | 아시아 · 유럽 · 북미 · 남미 · 아프리카 · 오세아니아 | P0 |
| 날씨 상태 | 맑음 · 구름 · 비 · 눈 · 안개 · 뇌우 | P0 |
| 기온 범위 | 듀얼 핸들 슬라이더 (-30°C ~ 50°C) | P1 |
| 시간대 | 지금 낮인 곳 / 지금 밤인 곳 | P1 |
| 즐겨찾기 | 즐겨찾기만 보기 | P1 |
| 반구 | 북반구 / 남반구 | P2 |

| ID | 요구사항 | 우선순위 |
|---|---|---|
| F6.1 | 다중 선택 및 조합(AND) 지원 | P0 |
| F6.2 | 적용된 필터를 칩(chip) 형태로 표시, 개별 ×로 해제 + "전체 해제" | P0 |
| F6.3 | 필터 결과 0건일 때 빈 상태(Empty State) UI + 필터 해제 유도 | P0 |
| F6.4 | 필터 조건 URL 쿼리 반영 (공유 가능) | P1 |
| F6.5 | 지구본에서 필터 제외된 핀은 제거가 아닌 **투명도 25% + 클릭 비활성** 처리 | P1 |
| F6.6 | 모바일에서는 하단 시트(bottom sheet)로 표시 | P0 |

### F7. 리스트 뷰 ★필수
| ID | 요구사항 | 우선순위 |
|---|---|---|
| F7.1 | 지구본↔리스트 뷰 토글 (상단바) | P0 |
| F7.2 | 카드 그리드: 랜드마크 썸네일 · 도시/국가 · 현재 기온 · 날씨 아이콘 · 현지 시각 · 낮/밤 배지 | P0 |
| F7.3 | 정렬: 기온 높은순/낮은순 · 도시명 가나다순 · 현지 시각순 · 대륙순 | P0 |
| F7.4 | 카드 클릭 → 지구본 뷰 전환 + 해당 지점 선택 | P0 |
| F7.5 | 카드 내 즐겨찾기 토글 | P1 |
| F7.6 | 로딩 시 스켈레톤 카드, 개별 카드 실패 시 카드 단위 에러 표시(전체 실패 아님) | P0 |
| F7.7 | 20개 초과 시 가상 스크롤 또는 페이지네이션 | P2 |
| F7.8 | 데스크톱에서 지구본 + 리스트 분할 뷰 (선택 사항) | P2 |

### F8. 로딩 화면 ★필수

**3단계 로딩 전략**

| 단계 | 대상 | UI |
|---|---|---|
| L1. 초기 부팅 | 지구 텍스처·모델·번들·랜드마크 JSON | **전체 화면 로더**: 어두운 우주 배경 위 회전하는 와이어프레임 지구, 하단 진행률 바 + `%` + 단계 라벨("지구 불러오는 중… / 랜드마크 배치 중… / 날씨 데이터 동기화 중…") |
| L2. 지점 선택 | 개별 좌표 날씨 API | **스켈레톤**: 상세 패널 골격 유지한 shimmer, 카메라 이동은 즉시 시작 |
| L3. 백그라운드 | 핀 배지 기온 갱신 | 상단 얇은 인디터미네이트 progress bar (화면 차단 없음) |

| ID | 요구사항 | 우선순위 |
|---|---|---|
| F8.1 | L1 로더는 실제 에셋 로딩 진행률과 연동 (가짜 프로그레스 금지) | P0 |
| F8.2 | 8초 초과 시 "네트워크가 느린 것 같아요. 조금만 더 기다려 주세요" 안내 노출 | P0 |
| F8.3 | 20초 초과 시 자동으로 **경량 모드(2D 폴백)** 제안 버튼 노출 | P1 |
| F8.4 | 최소 노출 시간 400ms 보장 (깜빡임 방지) | P1 |
| F8.5 | 로더에 날씨 관련 짧은 트리비아 로테이션 (체감 대기시간 감소) | P2 |
| F8.6 | `aria-live="polite"` + `role="status"`로 스크린리더 안내 | P0 |

### F9. 에러 화면 ★필수

**에러 유형별 처리 원칙: 국지적 실패는 국지적으로, 전역 실패만 전체 화면.**

| 코드 | 상황 | 표시 방식 | 문구 | 액션 |
|---|---|---|---|---|
| E-NET | 오프라인/네트워크 단절 | 전체 화면 | "인터넷 연결이 끊겼어요" | 다시 시도 · 온라인 복귀 시 자동 재시도 |
| E-API-5xx | Open-Meteo 서버 오류 | 상세 패널 내 인라인 | "날씨 정보를 불러오지 못했어요" | 다시 시도 (지수 백오프 3회) · 마지막 캐시 값 표시 |
| E-API-429 | 레이트 리밋 초과 | 토스트 | "요청이 많아 잠시 후 다시 시도해 주세요" | 60초 카운트다운 후 자동 재시도 |
| E-GEO | 지오코딩 실패 | 검색창 인라인 | "검색 중 문제가 발생했어요" | 재시도 |
| E-EMPTY | 결과 0건 (에러 아님) | 인라인 빈 상태 | "조건에 맞는 곳이 없어요" | 필터 해제 |
| E-WEBGL | WebGL 미지원/컨텍스트 로스트 | 전체 화면 → 폴백 | "이 브라우저에서는 3D 지구본을 표시할 수 없어요. 간편 모드로 계속할 수 있습니다." | 간편 모드로 계속 |
| E-404 | 존재하지 않는 딥링크 | 전용 페이지 | "찾으시는 장소가 없어요" | 홈으로 · 추천 랜드마크 |
| E-CRASH | 렌더 예외 (Error Boundary) | 전체 화면 | "예기치 못한 문제가 발생했어요" | 새로고침 · 에러 코드 표시 |

| ID | 요구사항 | 우선순위 |
|---|---|---|
| F9.1 | 모든 에러 화면은 **원인 + 사용자가 할 수 있는 행동**을 포함 (스택트레이스 노출 금지) | P0 |
| F9.2 | 최상위 React Error Boundary로 화이트스크린 방지 | P0 |
| F9.3 | API 실패 시 5xx/타임아웃에 한해 지수 백오프 재시도(1s/2s/4s), 4xx는 재시도 금지 | P0 |
| F9.4 | 마지막 성공 응답을 캐시해 실패 시 "N분 전 기준" 라벨과 함께 표시 | P1 |
| F9.5 | 에러 상태에서도 지구본 렌더링·회전은 유지 (전체 붕괴 방지) | P1 |
| F9.6 | 에러 발생률 로깅 (익명 집계) | P2 |

### F10. 설정 · 부가
| ID | 요구사항 | 우선순위 |
|---|---|---|
| F10.1 | 단위 (°C/°F, m/s·km/h·mph) | P1 |
| F10.2 | 언어 (한국어/영어) — Geocoding `language` 파라미터 및 UI 동기화 | P1 |
| F10.3 | 그래픽 품질 (자동/고/저) — 저품질 시 파티클·구름·안티앨리어싱 축소 | P1 |
| F10.4 | 사운드 On/Off | P2 |
| F10.5 | 즐겨찾기 목록 관리 (최대 20개) | P1 |

---

## 9. 데이터 설계

### 9.1 Open-Meteo Forecast API

```
GET https://api.open-meteo.com/v1/forecast
  ?latitude=48.8584&longitude=2.2945
  &current=temperature_2m,apparent_temperature,relative_humidity_2m,
           is_day,precipitation,weather_code,cloud_cover,
           surface_pressure,wind_speed_10m,wind_direction_10m
  &hourly=temperature_2m,weather_code,precipitation_probability
  &daily=weather_code,temperature_2m_max,temperature_2m_min,sunrise,sunset,uv_index_max
  &timezone=auto
  &forecast_days=7
```

- **API 키 불필요** (비상업적 무료 티어)
- `timezone=auto` → 좌표 기반 현지 타임존 자동 해석 (`utc_offset_seconds` 응답 포함) → **현지 시각 계산의 근거**
- `is_day` (1/0) → 배경 주야 판정 1차 신호, `sunrise`/`sunset`로 dawn/dusk 세분화
- `weather_code` (WMO) → 아이콘 및 배경 씬 매핑 키

### 9.2 Geocoding API

```
GET https://geocoding-api.open-meteo.com/v1/search?name={query}&count=8&language=ko&format=json
→ id, name, latitude, longitude, country, country_code, timezone, admin1, population, elevation
```

### 9.3 레이트 리밋 및 캐싱 전략

무료 티어 한도: **600/분, 5,000/시간, 10,000/일, 300,000/월**

| 전략 | 내용 |
|---|---|
| 배치 호출 | 다중 좌표를 콤마로 묶어 1회 요청 (`latitude=48.85,40.71&longitude=2.29,-74.0`) → 12개 랜드마크를 **1콜**로 처리 |
| 클라이언트 캐시 | 지점별 응답 TTL **10분** (SWR/TanStack Query). 그 사이 재선택은 네트워크 미사용 |
| 지오코딩 캐시 | 쿼리 문자열 기준 24시간 캐시 |
| 뷰포트 기반 조회 | 핀 배지 기온은 화면에 보이는 핀만 요청 |
| 폴링 | 자동 갱신은 10분 간격, 탭 비활성 시 중단 (Page Visibility API) |
| 서버 프록시 (선택) | 트래픽 증가 시 Edge Function에서 캐싱하여 클라이언트 호출량 축소 (v1.1 검토) |

### 9.4 랜드마크 데이터 스키마 (`landmarks.json`)

```json
{
  "id": "eiffel-tower",
  "name": { "ko": "에펠탑", "en": "Eiffel Tower" },
  "city": { "ko": "파리", "en": "Paris" },
  "country": { "ko": "프랑스", "en": "France" },
  "countryCode": "FR",
  "continent": "europe",
  "lat": 48.8584,
  "lon": 2.2945,
  "timezone": "Europe/Paris",
  "thumbnail": "/img/landmarks/eiffel-tower.webp",
  "iconModel": "/models/eiffel.glb",
  "priority": 1
}
```

### 9.5 WMO 날씨 코드 매핑 (부록 A 참조)
`weather_code` → `{ 한글 라벨, 아이콘, 배경 씬 그룹 }` 형태의 단일 매핑 테이블을 상수로 관리한다.

### 9.6 라이선스 / 출처 표기
Open-Meteo 데이터는 **CC BY 4.0** — 푸터에 다음 문구 필수:
> Weather data by [Open-Meteo.com](https://open-meteo.com) (CC BY 4.0)

---

## 10. 비기능 요구사항 (NFR)

| 영역 | 요구사항 |
|---|---|
| **성능** | 초기 번들 gzip ≤ 400KB(3D 라이브러리 제외) · 지구 텍스처 총합 ≤ 3MB (WebP/KTX2 압축, 2K) · 데스크톱 60FPS / 모바일 30FPS 이상 · LCP ≤ 2.5s |
| **반응형** | 데스크톱(≥1280): 3패널 · 태블릿(768–1279): 패널 오버레이 · 모바일(<768): 지구본 축소 + 하단 시트, 필터는 bottom sheet |
| **접근성** | WCAG 2.1 AA · 전 기능 키보드 접근 가능(핀은 Tab 순회 가능한 대체 리스트 제공) · 대비 4.5:1 · `prefers-reduced-motion` 대응 · 3D는 `aria-hidden`, 리스트 뷰가 접근성 등가 경로 |
| **브라우저** | Chrome/Edge/Safari/Firefox 최신 2개 버전 · iOS Safari 16+ · WebGL2 미지원 시 2D 폴백 |
| **SEO** | `/place/:slug` 정적 메타태그 + OG 이미지 · 리스트 뷰 SSR/프리렌더 |
| **보안/개인정보** | 계정·개인정보 수집 없음 · 위치 권한은 "내 위치" 기능 사용 시에만 요청(선택) · localStorage만 사용 |
| **국제화** | 한국어 기본, 영어 지원. 날짜/시간/숫자는 `Intl` API 사용 |

---

## 11. 기술 스택 제안

| 레이어 | 선택 | 비고 |
|---|---|---|
| 프레임워크 | React 18 + Vite (또는 Next.js — SEO 필요 시) | |
| 3D | Three.js + react-three-fiber + drei | 셰이더 커스터마이징 자유도 |
| 상태 | Zustand (UI 상태) + TanStack Query (서버 상태/캐싱/재시도) | F9.3 재시도 로직 내장 |
| 스타일 | Tailwind CSS + Framer Motion | |
| 텍스처 | NASA Blue Marble / Visible Earth (퍼블릭 도메인) | 라이선스 확인 필수 |
| 배포 | Vercel / Cloudflare Pages | |
| 분석 | Plausible 등 쿠키리스 | |

---

## 12. 마일스톤

| 단계 | 기간 | 산출물 |
|---|---|---|
| **M0. 스파이크** | 1주 | Three.js 지구본 + 핀 클릭 → Open-Meteo 응답 콘솔 출력 (기술 검증) |
| **M1. 코어** | 2주 | F1, F2, F3(기본), F8(L1/L2), F9(E-NET/E-API) — 랜드마크 12개 |
| **M2. 연출** | 2주 | F4 배경 매트릭스 전 조합, 파티클, 조명 동기화 |
| **M3. 탐색** | 1.5주 | F5 검색창, F6 필터, F7 리스트 뷰 |
| **M4. 완성도** | 1.5주 | 반응형, 접근성 감사, 성능 최적화, 폴백, 에러 전 케이스 |
| **M5. 베타** | 1주 | 딥링크·공유·분석 연동, 내부 QA, 출시 |

총 약 **9주** (1인 개발 기준, 여유 포함)

---

## 13. 리스크 및 대응

| 리스크 | 영향 | 대응 |
|---|---|---|
| 모바일 저사양 기기 프레임 드랍 | 이탈 | 자동 품질 강등(FPS 모니터링 → 파티클/구름 비활성), 저품질 텍스처 |
| Open-Meteo 무료 한도 초과 | 서비스 중단 | 배치 호출 + 10분 캐시 + Edge 프록시 캐싱, 429 전용 UX |
| 무료 티어는 비상업적 한정 | 수익화 불가 | v1은 비상업 운영. 상업화 시 유료 플랜 전환 예산 반영 |
| 지구 텍스처 용량 | LCP 저하 | KTX2/Basis 압축, 저해상도 → 고해상도 프로그레시브 로딩 |
| 배경 조합 수 폭증(4시간대 × 6날씨 = 24) | 개발 지연 | 시간대 그라디언트 + 날씨 오버레이의 **레이어 합성 방식**으로 조합 폭발 회피 |
| 랜드마크 이미지 저작권 | 법적 | 퍼블릭 도메인/CC0 소스만 사용, 또는 자체 3D 아이콘으로 대체 |

---

## 14. 열린 질문

1. 지구본 외 **임의 지점 클릭**(F2.6)을 v1에 포함할 것인가?
2. 랜드마크 썸네일은 실사 이미지 vs 일러스트/3D 아이콘 중 무엇으로 갈 것인가?
3. 사운드(F4.5)를 정체성으로 밀 것인가, 부가 기능으로 둘 것인가?
4. 도메인·서비스명 확정
5. SSR이 필요한 수준의 SEO 목표가 있는가? (Vite vs Next.js 결정에 직결)

---

## 부록 A. WMO Weather Code 매핑

| 코드 | 한글 라벨 | 배경 씬 그룹 |
|---|---|---|
| 0 | 맑음 | clear |
| 1 | 대체로 맑음 | clear |
| 2 | 부분적으로 흐림 | cloudy |
| 3 | 흐림 | cloudy |
| 45 | 안개 | fog |
| 48 | 상고대 안개 | fog |
| 51 / 53 / 55 | 약한 / 보통 / 강한 이슬비 | rain |
| 56 / 57 | 약한 / 강한 어는 이슬비 | rain |
| 61 / 63 / 65 | 약한 / 보통 / 강한 비 | rain |
| 66 / 67 | 약한 / 강한 어는 비 | rain |
| 71 / 73 / 75 | 약한 / 보통 / 강한 눈 | snow |
| 77 | 싸락눈 | snow |
| 80 / 81 / 82 | 약한 / 보통 / 강한 소나기 | rain |
| 85 / 86 | 약한 / 강한 소낙눈 | snow |
| 95 | 뇌우 | storm |
| 96 / 99 | 약한 / 강한 우박 동반 뇌우 | storm |

## 부록 B. 화면별 상태 체크리스트

모든 데이터 표시 영역은 아래 5가지 상태를 반드시 정의한다.

| 상태 | 정의 여부 |
|---|---|
| Empty (데이터 없음) | ✅ F6.3, F5.7 |
| Loading (로딩 중) | ✅ F8 |
| Partial (일부만 로드) | ✅ F7.6 |
| Error (실패) | ✅ F9 |
| Ideal (정상) | ✅ F3, F7 |
